const caixa = document.querySelector("#caixa");
const div = document.getElementsByTagName("div");
console.log(div);

// let mapa = new Map();

// mapa.set("curso", "JavaScript");
// mapa.set("canal", "CFBCursos");
// mapa.set("nota", 10);

// console.log(mapa);

// mapa.delete("canal");

// if (mapa.has("canal")) {
//     caixa.innerHTML = `A CHAVE EXISTE NA COLEÇÃO COM O VALOR ${mapa.get("canal")}.`;
// } else {
//     caixa.innerHTML = "A CHAVE NÃO EXISTE NA COLEÇÃO";
// }

// caixa.innerHTML = mapa.get("curso");
